# Installation

To install the app, clone or download the repository and optimally make a virtual environment and activate it. Then install requirements by

```
pip install -r requirements.txt
```

# Running the app

Run the app by

```
python run.py
```

It will start downloading the dataset, which may take half an hour. Using the app is easy (use keyboard shortcuts). The answers are saved into the file `answers.csv`.